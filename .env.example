# Server Environment Variables
NODE_ENV=development
PORT=5000
MONGODB_URI=mongodb://localhost:27017/learnit
SESSION_SECRET=your-session-secret-here

# CORS Configuration
# For development
CLIENT_URLS=http://localhost:3000,http://localhost:5173
# For production, add your Vercel URL, e.g.:
# CLIENT_URLS=https://your-vercel-app.vercel.app,http://localhost:3000,http://localhost:5173

# Google OAuth (if using)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback

# JWT Configuration (if using JWT)
JWT_SECRET=your-jwt-secret
JWT_EXPIRE=30d

# File Upload Configuration
UPLOAD_LIMIT=10MB

# Client Environment Variables (for Vite)
VITE_API_URL=http://localhost:5000/api  # Update this in production to your Render backend URL
